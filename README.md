# Pay Nest

A premium bilingual fintech landing page built with React, TypeScript, and Vite.

## Run locally

```bash
npm install
npm run dev
```

The language switcher supports English and Arabic, persists the selected language in localStorage, updates the document direction and language without a page reload, and keeps a shared information architecture across both locales.
